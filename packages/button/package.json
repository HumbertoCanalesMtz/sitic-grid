{
  "name": "@sitic-grid/button",
  "version": "1.0.0",
  "description": "Button component for sitic-grid library",
  "main": "dist/button.js",
  "types": "dist/button.d.ts",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.cjs -i ./src/styles.css -o ./dist/button.css --minify",
    "build": "rimraf dist && rspack build && npm run build:css",
    "dev": "rspack build --watch",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}