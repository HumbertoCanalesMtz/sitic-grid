{
  "name": "@sitic-grid/card",
  "version": "1.0.0",
  "description": "Card component for sitic-grid library",
  "main": "dist/card.js",
  "types": "dist/card.d.ts",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.cjs -i ./src/styles.css -o ./dist/card.css --minify",
    "build": "rimraf dist && rspack build && npm run build:css",
    "dev": "rspack build --watch",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}